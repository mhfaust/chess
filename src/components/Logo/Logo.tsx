import { Box, Text } from '@radix-ui/themes';
import { TextProps } from '@radix-ui/themes/dist/cjs/components/text';

export const Logo = (props: TextProps) => {
	return <Text size='9' {...props}>♞</Text>;
};
